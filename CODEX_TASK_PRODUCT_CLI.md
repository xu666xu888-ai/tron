# 🎯 Codex 任務指派：打造產品級 CLI 靚號生成器

## 📋 任務概述

將 TRON Vanity 地址生成器從技術原型升級為**產品級 CLI 工具**，提供完整的環境檢測、自動安裝、動態配置、實時監控和用戶友好的交互界面。

---

## 🔧 核心任務清單

### **階段 1：環境檢測與自動配置**（優先級：🔴 最高）

#### 1.1 系統信息收集模組
創建 `src/tron_vanity/system_info.py`：
- 檢測操作系統（Linux/Windows/macOS）
- 檢測 CPU 信息（核心數、架構、型號）
- 檢測 GPU 信息（型號、CUDA 版本、驅動版本、顯存）
- 檢測記憶體大小
- 檢測 Python 版本

#### 1.2 依賴檢測模組
創建 `src/tron_vanity/dependency_checker.py`：
- **Python 依賴**：
  - `cupy-cuda11x` 或 `cupy-cuda12x`（根據 CUDA 版本）
  - `tronpy`
  - `ecdsa`
  - `base58`
  - `rich`（用於 CLI UI）
  - `psutil`（用於系統監控）
  - `GPUtil`（用於 GPU 監控）
  
- **系統工具**：
  - CUDA Toolkit（檢測 `nvcc --version`）
  - NVIDIA Driver（檢測 `nvidia-smi`）
  - Python 3.8+（檢測 `python3 --version`）

#### 1.3 自動安裝模組
創建 `src/tron_vanity/auto_installer.py`：
- 缺少 Python 依賴時自動執行 `pip install`
- 提供清晰的安裝進度條
- 檢測 CUDA 版本並安裝對應的 CuPy
- 如果缺少 CUDA/Driver，提供安裝指引（不自動安裝系統級工具）

---

### **階段 2：動態配置與優化**（優先級：🟡 高）

#### 2.1 增強硬體配置系統
擴展現有的 `hardware_config.py`：
- 根據檢測到的 GPU 自動選擇最佳配置
- 如果沒有 GPU，自動切換到 CPU 模式
- 根據 CPU 核心數動態調整進程數
- 根據記憶體大小調整批次大小上限

#### 2.2 性能預估模組
創建 `src/tron_vanity/performance_estimator.py`：
- 根據硬體配置預估生成速度（addr/s）
- 根據靚號難度（末幾碼）預估所需時間
- 提供難度等級提示（簡單/中等/困難/極難）

**難度參考**：
- 末 1 碼：~58 次嘗試（瞬間）
- 末 2 碼：~3,364 次（< 1 秒）
- 末 3 碼：~195,112 次（< 1 秒）
- 末 4 碼：~11,316,496 次（~16 秒 @ 700k/s）
- 末 5 碼：~656,356,768 次（~15 分鐘 @ 700k/s）
- 末 6 碼：~38,068,692,544 次（~15 小時 @ 700k/s）
- 末 7 碼：~2,207,984,167,552 次（~36 天 @ 700k/s）
- 末 8 碼：~128,063,081,718,016 次（~5.8 年 @ 700k/s）❌ 不可行

---

### **階段 3：產品級 CLI UI**（優先級：🔴 最高）

#### 3.1 主程序入口
創建 `src/tron_vanity/cli.py`（主入口）：

**啟動流程**：
```
1. 顯示 ASCII Logo + 版本信息
2. 系統檢測階段（進度條）
   ├─ 檢測硬體信息
   ├─ 檢測依賴套件
   ├─ 自動安裝缺失依賴
   └─ 動態配置優化
3. 歡迎界面
   ├─ 顯示硬體配置摘要
   ├─ 顯示預估性能
   └─ 提示用戶輸入
4. 用戶輸入靚號
5. 難度評估與確認
6. 開始生成（實時監控）
7. 結果報告
```

#### 3.2 實時監控 UI
使用 `rich` 庫創建動態 CLI UI：

**監控面板佈局**：
```
╔══════════════════════════════════════════════════════════════╗
║  🎯 TRON Vanity Address Generator v2.0                      ║
║  目標靚號: T...88888                                         ║
╠══════════════════════════════════════════════════════════════╣
║  ⏱️  運行時間: 00:05:23                                      ║
║  🔍 已檢查: 2,345,678 個地址                                 ║
║  ⚡ 當前速度: 696,344 addr/s                                 ║
║  📊 預估剩餘: ~8 分鐘                                         ║
╠══════════════════════════════════════════════════════════════╣
║  💻 CPU 使用率: ████████░░ 82%                               ║
║  🎮 GPU 使用率: ██████████ 98%                               ║
║  🌡️  GPU 溫度: 76°C                                          ║
║  💾 GPU 記憶體: 8.2 / 22.0 GiB                               ║
╠══════════════════════════════════════════════════════════════╣
║  📈 性能曲線: [實時圖表]                                      ║
║  ▂▃▅▆█▇▆▅▄▃▂▁▂▃▅▆█▇▆▅▄▃▂▁▂▃▅▆█▇▆▅▄▃▂▁                        ║
╠══════════════════════════════════════════════════════════════╣
║  🎉 已找到 0 個匹配地址                                       ║
║  [按 Ctrl+C 停止搜索]                                        ║
╚══════════════════════════════════════════════════════════════╝
```

**監控數據**：
- 運行時間（實時更新）
- 已檢查地址數量
- 當前生成速度（每秒更新）
- 預估剩餘時間（動態調整）
- CPU 使用率（`psutil`）
- GPU 使用率（`GPUtil` 或 `nvidia-smi`）
- GPU 溫度
- GPU 記憶體使用
- 性能曲線（最近 30 秒的速度變化）
- 已找到的匹配地址列表

#### 3.3 結果報告
找到靚號後顯示：
```
╔══════════════════════════════════════════════════════════════╗
║  🎉 成功找到靚號！                                            ║
╠══════════════════════════════════════════════════════════════╣
║  地址: TXyz...88888                                          ║
║  私鑰: 0x1234...abcd                                         ║
║  ⚠️  請立即備份私鑰！                                         ║
╠══════════════════════════════════════════════════════════════╣
║  統計信息:                                                    ║
║  - 總耗時: 15 分 32 秒                                        ║
║  - 總檢查: 648,234,567 個地址                                ║
║  - 平均速度: 695,432 addr/s                                  ║
║  - 難度: 1 / 11,316,496 (末 4 碼)                            ║
╠══════════════════════════════════════════════════════════════╣
║  結果已保存到: vanity_result_20251013_100523.txt            ║
╚══════════════════════════════════════════════════════════════╝
```

---

### **階段 4：用戶交互優化**（優先級：🟡 高）

#### 4.1 靚號輸入驗證
- 支援末幾碼輸入（如 `88888`、`david`）
- 自動轉換為 Base58 字符集（排除 0OIl）
- 提供難度評估和預估時間
- 如果難度過高（> 6 碼），警告用戶並要求確認

#### 4.2 交互式配置
提供可選的高級配置：
```
是否使用 GPU 加速？[Y/n]: Y
批次大小（自動/手動）[auto]: auto
最大運行時間（分鐘，0=無限制）[0]: 30
找到幾個結果後停止？[1]: 1
```

#### 4.3 結果保存
自動保存結果到文件：
```
vanity_result_20251013_100523.txt
---
地址: TXyz...88888
私鑰: 0x1234...abcd
生成時間: 2025-10-13 10:05:23
總耗時: 15 分 32 秒
總檢查: 648,234,567 個地址
平均速度: 695,432 addr/s

⚠️ 警告：請妥善保管私鑰，切勿洩露！
```

---

## 🎨 技術實現建議

### **使用的庫**
```python
# CLI UI
from rich.console import Console
from rich.progress import Progress, SpinnerColumn, BarColumn, TextColumn
from rich.table import Table
from rich.panel import Panel
from rich.live import Live
from rich.layout import Layout

# 系統監控
import psutil  # CPU/記憶體監控
import GPUtil  # GPU 監控（或使用 pynvml）

# 其他
import time
import signal  # 處理 Ctrl+C
import json
from datetime import datetime, timedelta
```

### **關鍵功能**

1. **優雅的 Ctrl+C 處理**
   ```python
   def signal_handler(sig, frame):
       console.print("\n[yellow]正在停止搜索...[/yellow]")
       # 保存當前進度
       # 顯示統計信息
       sys.exit(0)
   
   signal.signal(signal.SIGINT, signal_handler)
   ```

2. **實時更新 UI**
   ```python
   with Live(layout, refresh_per_second=4) as live:
       while not found:
           # 更新監控數據
           # 更新 UI
           time.sleep(0.25)
   ```

3. **性能曲線**
   ```python
   from collections import deque
   speed_history = deque(maxlen=30)  # 保留最近 30 秒
   # 使用 rich 的 Sparkline 或自定義 ASCII 圖表
   ```

---

## 📦 文件結構

```
src/tron_vanity/
├── cli.py                    # 主入口（新增）
├── system_info.py            # 系統信息檢測（新增）
├── dependency_checker.py     # 依賴檢測（新增）
├── auto_installer.py         # 自動安裝（新增）
├── performance_estimator.py  # 性能預估（新增）
├── monitor.py                # 實時監控（新增）
├── ui_components.py          # UI 組件（新增）
├── hardware_config.py        # 硬體配置（已有，需擴展）
├── gpu_addr.py               # GPU 生成（已有）
├── validate.py               # 驗證（已有）
└── ...
```

---

## 🚀 啟動方式

```bash
# 方式 1：直接運行
python -m tron_vanity.cli

# 方式 2：安裝後運行
pip install -e .
tron-vanity

# 方式 3：一鍵啟動腳本
./run.sh
```

---

## ✅ 驗收標準

1. ✅ 在全新環境下可以一鍵運行（自動安裝依賴）
2. ✅ 自動檢測並配置硬體（CPU/GPU）
3. ✅ 提供美觀的 CLI UI（使用 rich）
4. ✅ 實時監控性能（速度、GPU/CPU 負載、溫度）
5. ✅ 支援末幾碼靚號輸入（如 88888、david）
6. ✅ 提供難度評估和預估時間
7. ✅ 優雅處理 Ctrl+C（保存進度）
8. ✅ 自動保存結果到文件
9. ✅ 性能達到 600k+ addr/s（GPU 模式）
10. ✅ 用戶體驗流暢，像是一個產品

---

## 🎯 額外優化建議（來自之前的測試）

1. **調整 Window4 門檻**：從 131072 提升到 196608，避免小批次性能退化
2. **多 Stream 並行**：測試 8-16 條 streams，預期提升 20-30%
3. **前綴搜索**：不需要執著修復，專注於末幾碼搜索（更實用）

---

## 📝 開發順序建議

1. **第一天**：環境檢測 + 自動安裝（階段 1）
2. **第二天**：CLI UI 框架 + 基本監控（階段 3.1-3.2）
3. **第三天**：實時監控 + 性能曲線（階段 3.2）
4. **第四天**：用戶交互 + 結果保存（階段 3.3 + 4）
5. **第五天**：性能優化 + 測試（階段 2 + 優化建議）

---

## 📊 當前性能基準（供參考）

根據最新的 benchmark 測試結果：

### **硬體配置**
- GPU: NVIDIA L4
- SM: 58
- Compute Capability: 8.9
- VRAM: 22.0 GiB

### **性能數據**
| 批次大小 | Window4 | Baseline | 狀態 |
|---------|---------|----------|------|
| 32k | 314k addr/s | 426k addr/s | ⚠️ 退化 -26% |
| 64k | 579k addr/s | 547k addr/s | ✅ 提升 +6% |
| 131k | 651k addr/s | 636k addr/s | ✅ 提升 +2% |
| **262k** | **696k addr/s** | 647k addr/s | 🎉 **最佳 +8%** |

**最佳性能**：262k 批次達到 **696k addr/s**（比初始 200k 提升 **248%**）

---

**預期成果**：一個可以直接交付給用戶的產品級 TRON 靚號生成器，具備自動配置、實時監控、用戶友好的 CLI UI，性能達到 600k+ addr/s。