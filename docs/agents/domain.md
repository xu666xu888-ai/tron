# Domain docs

This is a single-context repository. Read root `CONTEXT.md` and relevant `docs/adr/`
files when present; if absent, proceed without creating speculative documents.
Use documented domain terminology and flag conflicting architectural decisions.
